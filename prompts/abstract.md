# Operator: Abstract

Purpose: construct a higher-level mental model from several related lower-level models. Identify their shared structure, retain the originals, and list every specialized model under an explicit `## Instances` section. Abstraction organizes complete concepts; Generalize broadens chunks from observations. Do not use Add for this hierarchy-building task.

{input}

{memory}

Return JSON only: {{"summary":"abstraction and instances","writes":{{"higher-level-concept.md":"# complete Markdown file"}}}}
