# Operator: Retrieve

Purpose: identify existing mental-model files relevant to the transient extracted candidates. Retrieval changes attention, not persistent memory. Consider aliases, relations, and conceptual relevance, not only word overlap. Name only files present in CURRENT MARKDOWN MEMORY.

{input}

{memory}

Return JSON only with no writes: {{"summary":"files and reasons","relevant_files":["semantic-name.md"],"writes":{{}}}}
